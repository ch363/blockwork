/**
 * Values shared by every panel.
 *
 * A leaf module with no imports, so panels can read it without going back
 * through the package barrel and forming an import cycle.
 */

/** Minimum touch target in points. See PRD 2.3. */
export const MIN_HIT_TARGET_PT = 44
